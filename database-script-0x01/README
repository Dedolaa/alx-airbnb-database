# Airbnb Database Sample Data

## 📄 File: `seed.sql`

This script inserts sample data into the Airbnb database schema. It helps simulate a real-world use case for testing and development.

---

## ✅ What It Does

The script populates the following tables:

- `User`: Adds 3 users (host, guest, admin)
- `Property`: 2 properties listed by the host
- `Booking`: 2 bookings by the guest
- `Payment`: 1 confirmed payment
- `Review`: 1 review from the guest
- `Message`: 1 message from guest to host

---

## ▶️ How to Use

1. Ensure you have already created the schema using `schema.sql`.

2. Open your SQL client or terminal.

3. Run the `seed.sql` script:

   ```sql
   \i path/to/seed.sql         -- for PostgreSQL
   SOURCE path/to/seed.sql;    -- for MySQL
